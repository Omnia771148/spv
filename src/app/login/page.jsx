import { LoginInfoPage } from "./loginInfoPage";

// This is the Next.js page entry point. 
// It simply renders the state container component (LoginInfoPage).
export default function Page() {
    return <LoginInfoPage />;
}